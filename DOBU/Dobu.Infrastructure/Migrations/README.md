# Banco de dados

A estrutura da entrega é criada pelo arquivo `script_bd.sql`, executado no Azure Database for PostgreSQL Flexible Server conforme o README da raiz. As migrations específicas de Oracle foram removidas para manter a solução exclusivamente em PostgreSQL, conforme a opção App Service + Banco PaaS.
